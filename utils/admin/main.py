import logging
from aiogram import Dispatcher, Bot
from utils.admin.notify_admins import (app_notify)


async def main(dp: Dispatcher, bot: Bot):
    # text = "Выполняю функцию main()"
    # await app_notify(bot, text)
    pass
